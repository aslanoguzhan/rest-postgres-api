pipeline {
    agent any
   
    stages {
        stage("build project") {
            steps {
               // git 'https://github.com/aslanoguzhan/rest-postgres-api.git'
                echo "Java VERSION"
                sh 'java -version'
                echo "Maven VERSION"
                sh 'mvn -version'
                echo 'building project...'
                sh "mvn compile"
                sh "mvn package"
                //sh "mvn test"
                sh "mvn clean install"
            }
        }
    }
}
