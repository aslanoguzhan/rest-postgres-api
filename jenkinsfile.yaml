pipeline {
    agent any
   
    stages {
        stage("build project") {
            steps {
               // git 'https://github.com/aslanoguzhan/rest-postgres-api.git'
                echo "Java VERSION"
            
                echo "Maven VERSION"
                
                echo 'building project...'
                
            }
        }
    }
}
